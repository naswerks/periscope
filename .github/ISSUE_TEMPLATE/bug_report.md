---
name: Bug report
about: Something the host does that it should not, or does not do that it should
labels: bug
---

## What happened

<!-- What you ran, what you expected, what you observed. -->

## `periscope status`

<!-- Paste the output. It names the version, the link state, the credential kind, the workspace
     mode and every setting with its source; secrets are never printed. -->

## The controller

<!-- Which controller the host was paired to: the reference controller under examples/, or your
     own. If your own, which of its transports the failure involves (the link, the decision
     endpoint, a bulk sink, the pair route). -->

## Environment

- Operating system:
- Node version (`node --version`):
- Package version (`periscope version`):
